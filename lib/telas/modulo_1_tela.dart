import 'package:flutter/material.dart';

class Modulo1Tela extends StatelessWidget {
  const Modulo1Tela({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Tela do Módulo 1',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}