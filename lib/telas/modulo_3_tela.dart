import 'package:flutter/material.dart';

class Modulo3Tela extends StatelessWidget {
  const Modulo3Tela({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Tela do Módulo 3',
        style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
      ),
    );
  }
}